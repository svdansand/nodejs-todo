apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodejs-todo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: nodejs-todo-app
  template:
    metadata:
      labels:
        app: nodejs-todo-app
    spec:
      containers:
      - name: nodejs-todo
        image: gcr.io/dansand/github.com/svdansand/nodejs-todo
        resources:
          requests:
            memory: "64Mi"
            cpu: "100m"
          limits:
            memory: "128Mi"
            cpu: "250m" 
        ports:
        - containerPort: 8080
        env:
        - name: MONGO_DBCONNECTION
          value: "mongodb://my-mongodb-replicaset-0.my-mongodb-replicaset.default.svc.cluster.local:27017/todo"
---
apiVersion: v1
kind: Service
metadata:
 name: nodejs-todo
 labels:
   name: nodejs-todo
spec:
 ports:
 - port: 80
   targetPort: 8080
 type: LoadBalancer
 selector:
   app: nodejs-todo-app